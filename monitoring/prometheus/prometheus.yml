global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'marketnote'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - 'users.marketnote.store:443'
    scheme: https